{
  "name": "website",
  "version": "1.0.0",
  "description": "**Spreading good vibes, one message at a time.**",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:hubs && npm run build:listicles && npm run build:messages && npm run build:layout && npm run build:assets && npm run build:sitemap",
    "build:hubs": "node scripts/build/generate-hubs.js",
    "build:listicles": "node scripts/build/generate-listicles.js",
    "build:messages": "node scripts/build/generate-messages.js",
    "build:layout": "node scripts/build/sync-layout.js",
    "build:assets": "node scripts/build/version-assets.js",
    "build:sitemap": "node scripts/build/generate-sitemap.js",
    "generate-pins": "node scripts/marketing/generate-pins.js",
    "pin-post": "python scripts/marketing/pinterest_poster.py"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheVibeCheckProject/Website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/TheVibeCheckProject/Website/issues"
  },
  "homepage": "https://www.thevibecheckproject.com",
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "cheerio": "^1.2.0",
    "playwright": "^1.58.2"
  }
}
